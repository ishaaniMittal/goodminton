import React from "react";
import styles from "../common/styles";
import {Button, Text, View} from "react-native";

class DrillTrainingInformation extends React.Component {



 /*   handleButtonClick() {
        if (this.state.buttonTitle === "Start Recording") {
            this.setState({
                ...this.state,
                buttonTitle: "Stop Recording"
            })
        }
        else {
            this.setState({
                ...this.state,
                buttonTitle: "Start Recording"
            })
        }
    }*/


    render() {
        return (<View>

        </View>);
    }
}

export default DrillTrainingInformation;